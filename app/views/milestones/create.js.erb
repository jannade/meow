
// close the modal
buttonMilestone = document.querySelector('#milestone-modal-<%= @milestone.goal.id%> button[data-dismiss="modal"]')
buttonMilestone.click()

// append the milestone
milestoneList = document.querySelector('.card-milestones.goal-<%=@milestone.goal.id%>')
newMilestone = '<%= j render "milestones/newmilestone", milestone: @milestone %>';

milestoneList.insertAdjacentHTML('beforebegin', newMilestone);
